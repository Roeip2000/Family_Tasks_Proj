<?xml version="1.0" encoding="utf-8"?>
<!-- מסך סריקת QR לילד — כפתור יחיד שפותח את מצלמת הסורק (ZXing).
     Fragment שמוצג בתוך fragmentContainer של activity_main.xml.
     ChildQRLoginFragment.java אחראי על פענוח, בדיקה מול Firebase, ושמירת סשן. -->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_card"
    tools:context=".Child_Login.ChildQRLoginFragment">

    <!-- הסבר קצר מעל הכפתור -->
    <TextView
        android:id="@+id/tvQrHint"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="20dp"
        android:gravity="center"
        android:text="@string/child_qr_scan_prompt"
        android:textColor="@color/text_secondary"
        android:textSize="14sp"
        app:layout_constraintBottom_toTopOf="@id/btnScanQR"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/btnScanQR"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:backgroundTint="@color/primary"
        android:paddingHorizontal="32dp"
        android:paddingVertical="14dp"
        android:text="@string/scan_qr"
        android:textAllCaps="false"
        android:textColor="@color/white"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
